<%@utilities.each do |utility|%>
  <%= link_to user_utility_path(user_id: current_user.id, id: utility.id) do %>
    <div class="utility">
      <%= image_tag utility_icon(utility, 40), class: "icon" %>
      <div class="utility__detail">
        <p class="utility__name" > <%= utility.name %> </p>
        <p class="utility__badge" > <%= utility.utility_payments.count %> </p>
      </div>
    </div>
  <%end%>
<%end%>